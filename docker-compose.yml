services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: scaffold
      POSTGRES_PASSWORD: scaffold
      POSTGRES_DB: scaffold
    ports:
      - "5432:5432"
    volumes:
      - scaffold_pg:/var/lib/postgresql/data

volumes:
  scaffold_pg:
